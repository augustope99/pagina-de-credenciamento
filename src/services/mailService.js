// mailService.js (placeholder para envio via Graph API ou SMTP)
export async function sendEmail({ subject, body, to }) {
  // Implemente a lógica de envio de e-mail aqui
  // Exemplo: await fetch('/api/send', { method: 'POST', body: JSON.stringify({ subject, body, to }) });
  return Promise.resolve();
}
